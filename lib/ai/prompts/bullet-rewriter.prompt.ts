export const BULLET_REWRITER_PROMPT = `BULLET REWRITES:

Rewrite only weak or poorly targeted resume bullets.
Only rewrite bullets that materially benefit from improvement.

For every rewrite return:

- original
- improved
- reason

Keep the original factual meaning.

Do not add a metric unless the original resume already contains it.`;
